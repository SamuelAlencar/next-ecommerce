/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'samuel-alencar-portifolio.vercel.app',
      'xesque.rocketseat.dev',
      'http://localhost:3000/',
    ],
  },
}

export default nextConfig
